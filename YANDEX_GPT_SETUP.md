# 🔧 Настройка YandexGPT для Telegram Mini App

## ✅ Изменения завершены!

Проект успешно переведен с **g4f (ChatGPT)** на **YandexGPT**. Теперь приложение будет работать во всех странах СНГ без VPN!

---

## 📋 Что было изменено:

### Backend:
- ✅ Удалена библиотека `g4f` и её конфигурация
- ✅ Добавлена интеграция с **YandexGPT API**
- ✅ Обновлена функция `ai_interpretation()` для использования YandexGPT
- ✅ Добавлены переменные окружения `YANDEX_API_KEY` и `YANDEX_FOLDER_ID`
- ✅ Добавлена библиотека `aiohttp` для HTTP запросов

### Конфигурация:
- ✅ `requirements.txt` - заменен `g4f` на `aiohttp`
- ✅ `.env.example` - добавлены переменные для YandexGPT

---

## 🚀 Как получить YandexGPT API ключ

### Шаг 1: Регистрация в Yandex Cloud

1. Перейдите на https://cloud.yandex.ru/
2. Нажмите **"Войти в консоль"** или **"Начать бесплатно"**
3. Авторизуйтесь через Яндекс аккаунт (или создайте новый)

### Шаг 2: Создание каталога (Folder)

1. В консоли Yandex Cloud нажмите **"Создать каталог"**
2. Введите название (например, "tarot-app")
3. Нажмите **"Создать"**
4. **Скопируйте ID каталога** - это будет ваш `YANDEX_FOLDER_ID`

   Пример: `b1g1234567890abcdefg`

### Шаг 3: Создание API ключа

1. В меню слева выберите **"Сервисные аккаунты"**
2. Нажмите **"Создать сервисный аккаунт"**
3. Введите имя (например, "tarot-bot")
4. Добавьте роль: **`ai.languageModels.user`**
5. Нажмите **"Создать"**
6. Откройте созданный аккаунт
7. Нажмите **"Создать новый ключ"** → **"API-ключ"**
8. **Скопируйте ключ** - это будет ваш `YANDEX_API_KEY`

   Пример: `AQVNabcdefg1234567890_KLMNOPQRST`

⚠️ **ВАЖНО:** Сохраните ключ сразу - он показывается только один раз!

### Шаг 4: Включение YandexGPT API

1. В меню слева выберите **"API Gateway"** или **"Billing"**
2. Активируйте пробный период (если еще не активирован)
3. Убедитесь, что сервис **Yandex Foundation Models** включен

---

## ⚙️ Настройка переменных окружения

### Локальная разработка:

Создайте файл `backend/.env`:

```env
FRONTEND_URL=http://localhost:3000
PORT=8000
YANDEX_API_KEY=AQVNabcdefg1234567890_KLMNOPQRST
YANDEX_FOLDER_ID=b1g1234567890abcdefg
```

### Railway (Production):

В настройках проекта Railway добавьте переменные:

1. Откройте ваш проект на https://railway.app
2. Перейдите в **Variables**
3. Добавьте:
   - `YANDEX_API_KEY` = ваш API ключ
   - `YANDEX_FOLDER_ID` = ID вашего каталога
   - `FRONTEND_URL` = URL Vercel (после деплоя)

---

## 💰 Стоимость YandexGPT

### Бесплатный tier (Trial):
- **1000 запросов** в день
- Идеально для разработки и тестирования

### Платный tier:
- **~0.0012₽** за 1 токен (~1 символ)
- Средний расклад Таро: **~500-800 токенов** = ~0.6-1₽
- **1000 гаданий ≈ 600-1000₽** в месяц

**Для начала хватит бесплатного tier!**

---

## 🧪 Тестирование

### Локальный запуск:

```bash
# Backend
cd backend
pip install -r requirements.txt
python main.py

# Frontend
cd frontend
npm install
npm start
```

### Проверка работы YandexGPT:

1. Откройте http://localhost:3000
2. Задайте вопрос и выберите расклад
3. Нажмите "Начать гадание"
4. Если все настроено правильно - получите интерпретацию от YandexGPT!

### Проверка ошибок:

Если видите сообщение:
- **"Конфигурация YandexGPT не настроена"** → проверьте `.env` файл
- **"Ошибка YandexGPT API (статус 401)"** → неверный API ключ
- **"Ошибка YandexGPT API (статус 403)"** → проверьте роли сервисного аккаунта
- **"Ошибка YandexGPT API (статус 400)"** → неверный FOLDER_ID

---

## 📦 Деплой

### 1. Railway (Backend)

```bash
cd backend
railway login
railway up
```

Или через Railway Dashboard:
1. New Project → Deploy from GitHub
2. Root Directory: `backend/`
3. Environment Variables:
   - `YANDEX_API_KEY`
   - `YANDEX_FOLDER_ID`
   - `FRONTEND_URL` (добавить после деплоя Vercel)

### 2. Vercel (Frontend)

```bash
cd frontend
vercel
```

Environment Variables в Vercel:
- `REACT_APP_API_URL` = URL Railway backend

### 3. Telegram Bot

1. Создайте бота через @BotFather
2. Настройте Mini App URL:
   ```
   /setappurl
   [выберите бота]
   https://your-vercel-url.vercel.app
   ```

---

## 🔍 Отладка

### Проверка логов Railway:

```bash
railway logs
```

Ищите строки:
- `YandexGPT API` - запросы к YandexGPT
- `Ошибка` - ошибки интерпретации

### Мониторинг использования YandexGPT:

1. Откройте https://cloud.yandex.ru/
2. Перейдите в **Billing** → **Детализация**
3. Смотрите использование **Foundation Models**

---

## ✨ Преимущества YandexGPT

✅ **Работает в СНГ без VPN**
✅ **Отличное понимание русского языка**
✅ **Бесплатный tier для старта**
✅ **Стабильная работа**
✅ **Официальный API с поддержкой**

---

## 📞 Поддержка

### Документация YandexGPT:
https://cloud.yandex.ru/docs/foundation-models/

### API Reference:
https://cloud.yandex.ru/docs/foundation-models/api-ref/

### Помощь Yandex Cloud:
https://cloud.yandex.ru/support

---

## 🎯 Следующие шаги:

1. ✅ Получите API ключ YandexGPT (инструкции выше)
2. ✅ Создайте `.env` файл и добавьте ключи
3. ✅ Протестируйте локально
4. ✅ Задеплойте на Railway + Vercel
5. ✅ Настройте Telegram бота
6. 🚀 Запустите Mini App!

---

**Готово! Ваше приложение теперь работает с YandexGPT и доступно в СНГ! 🎉**
