# 🚀 g4f с прокси - Бесплатный GPT-4 для СНГ

## ✅ Интеграция g4f завершена!

Проект теперь использует **g4f (GPT4Free)** - бесплатный доступ к GPT-4 с поддержкой прокси!

---

## 🎯 Что такое g4f?

**g4f** - это библиотека, которая даёт **бесплатный** доступ к GPT-4 и другим AI моделям через различные публичные провайдеры.

### Преимущества:
- ✅ **100% БЕСПЛАТНО** - не нужен API ключ!
- ✅ **Качество GPT-4** - лучшая модель OpenAI
- ✅ **Поддержка прокси** - работает даже при блокировках
- ✅ **Автоматический выбор провайдера** - g4f сам находит рабочий
- ✅ **Отличное понимание русского языка**

---

## 🔧 Вариант 1: БЕЗ прокси (попробуйте сначала!)

Некоторые провайдеры g4f работают даже в СНГ без VPN!

### Шаг 1: Установите зависимости

```powershell
cd "C:\Users\Vbelo\OneDrive\Desktop\luvo taro\taro1 (1)\taro1\backend"
python -m pip install -r requirements.txt
```

### Шаг 2: Запустите backend

```powershell
python main.py
```

При запуске вы увидите:
```
ℹ️ Прокси не настроен, используется прямое подключение
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 3: Протестируйте

1. Откройте http://localhost:3000
2. Задайте вопрос и выберите расклад
3. Нажмите "Начать гадание"
4. g4f попробует разные провайдеры автоматически

**Если работает - отлично! Прокси не нужен.**
**Если не работает - переходите к Варианту 2.**

---

## 🌐 Вариант 2: С ПРОКСИ (для заблокированных регионов)

Если g4f не работает без прокси, вам нужен HTTP/SOCKS5 прокси сервер.

### Где взять прокси?

#### Бесплатные прокси:
- https://www.proxy-list.download/
- https://hidemy.name/ru/proxy-list/
- https://spys.one/free-proxy-list/

⚠️ **Внимание:** Бесплатные прокси часто нестабильны!

#### Платные прокси (рекомендуется):
- **Webshare.io** - от $2.99/месяц, 10 прокси
- **ProxyRack** - от $50/месяц, безлимит
- **Bright Data** - от $500/месяц, профессиональный

#### VPN как прокси:
Если у вас есть VPN (Shadowsocks, V2Ray, etc.):
- Shadowsocks: `socks5://127.0.0.1:1080`
- V2Ray: `socks5://127.0.0.1:10808`
- Clash: обычно `http://127.0.0.1:7890`

---

## ⚙️ Настройка прокси

### Шаг 1: Откройте файл `.env`

Файл находится здесь:
```
C:\Users\Vbelo\OneDrive\Desktop\luvo taro\taro1 (1)\taro1\backend\.env
```

### Шаг 2: Добавьте прокси

Раскомментируйте и настройте строку `PROXY_URL`:

**Для HTTP прокси:**
```env
PROXY_URL=http://proxy.example.com:8080
```

**Для HTTP прокси с авторизацией:**
```env
PROXY_URL=http://username:password@proxy.example.com:8080
```

**Для SOCKS5 прокси:**
```env
PROXY_URL=socks5://127.0.0.1:1080
```

**Для SOCKS5 с авторизацией:**
```env
PROXY_URL=socks5://username:password@proxy.example.com:1080
```

### Примеры:

#### Пример 1: Бесплатный прокси
```env
PROXY_URL=http://45.123.456.78:8080
```

#### Пример 2: Shadowsocks (локальный)
```env
PROXY_URL=socks5://127.0.0.1:1080
```

#### Пример 3: Платный прокси Webshare
```env
PROXY_URL=http://myuser:mypass@proxy.webshare.io:80
```

### Шаг 3: Перезапустите backend

```powershell
# Ctrl+C чтобы остановить
python main.py
```

При запуске вы должны увидеть:
```
✅ Прокси настроен: http://proxy.example.com:8080
INFO:     Uvicorn running on http://0.0.0.0:8000
```

---

## 🧪 Тестирование

### 1. Проверьте backend логи

После запуска гадания смотрите терминал backend.
Если видите ошибки типа:
- `ConnectionError` - проблема с прокси
- `TimeoutError` - прокси медленный или не работает
- `ProxyError` - неверные данные прокси

### 2. Проверьте frontend

Если всё настроено правильно:
- Гадание создаётся
- Карты переворачиваются
- Появляется толкование от GPT-4

### 3. Если не работает

Попробуйте другой прокси из списка!

---

## 🔍 Поиск рабочего прокси

### Скрипт для проверки прокси:

Создайте файл `test_proxy.py`:

```python
import requests

proxies = [
    "http://45.123.456.78:8080",
    "socks5://127.0.0.1:1080",
    # добавьте свои прокси
]

for proxy in proxies:
    try:
        response = requests.get(
            "https://api.openai.com",
            proxies={"http": proxy, "https": proxy},
            timeout=5
        )
        print(f"✅ {proxy} - РАБОТАЕТ")
    except Exception as e:
        print(f"❌ {proxy} - НЕ РАБОТАЕТ: {e}")
```

Запустите:
```powershell
python test_proxy.py
```

Используйте прокси с ✅ в `.env` файле!

---

## 📦 Деплой на Railway с прокси

### В Railway Dashboard:

1. Откройте ваш проект
2. Перейдите в **Variables**
3. Добавьте:
   - `PROXY_URL` = ваш прокси URL
   - `FRONTEND_URL` = URL Vercel

**Важно:** Используйте платный прокси для production, бесплатные ненадёжны!

---

## 💡 Альтернативные решения

Если прокси не работает или слишком дорого:

### Вариант A: Groq API (Рекомендуется!)
- ✅ Полностью бесплатно
- ✅ Работает в СНГ без VPN
- ✅ Очень быстрый
- Регистрация: https://console.groq.com/

**Хотите, чтобы я переделал на Groq?** Это займёт 2 минуты!

### Вариант B: OpenRouter
- ✅ Бесплатные модели доступны
- ✅ Работает в СНГ
- Регистрация: https://openrouter.ai/

### Вариант C: Локальная модель (Ollama)
- ✅ Полностью автономно
- ❌ Требует мощный компьютер
- ❌ Медленнее облачных моделей

---

## 📊 Сравнение провайдеров

| Провайдер | Бесплатно? | Нужен прокси? | Скорость | Качество |
|-----------|------------|---------------|----------|----------|
| **g4f БЕЗ прокси** | ✅ Да | ❓ Зависит | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **g4f С прокси** | ✅ Да* | ✅ Да | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Groq** | ✅ Да | ❌ Нет | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **DeepSeek** | ⚠️ Лимиты | ❌ Нет | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

*Прокси может быть платным

---

## ❓ FAQ

### Q: Какой прокси выбрать?
**A:** Для локальной разработки - бесплатный. Для production - платный (Webshare, ProxyRack).

### Q: Можно ли использовать VPN вместо прокси?
**A:** Да! Если VPN предоставляет HTTP/SOCKS5 прокси локально.

### Q: g4f не работает даже с прокси?
**A:** Попробуйте **Groq API** - он работает гарантированно и бесплатно!

### Q: Сколько стоит прокси?
**A:** От $3/месяц (Webshare) до $500/месяц (профессиональные сервисы).

### Q: g4f работает в России?
**A:** С прокси - да. Без прокси - зависит от провайдера, который выберет g4f.

---

## 🎯 Рекомендация

Если g4f не работает без прокси и вы не хотите тратиться на прокси:

**Используйте Groq API!**
- 100% бесплатно
- Работает в СНГ
- Супер быстрый
- Не нужен прокси

Напишите мне, если хотите переключиться на Groq! 🚀

---

## 📞 Полезные ссылки

- **g4f GitHub:** https://github.com/xtekky/gpt4free
- **Список бесплатных прокси:** https://www.proxy-list.download/
- **Webshare прокси:** https://www.webshare.io/
- **Groq Console:** https://console.groq.com/

---

**Готово! Теперь g4f настроен с поддержкой прокси! 🎉**

Попробуйте сначала БЕЗ прокси, возможно заработает сразу!
