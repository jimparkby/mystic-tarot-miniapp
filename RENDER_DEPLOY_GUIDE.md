# 🚀 Деплой Tarot Mini App на бесплатных сервисах

## 🎁 Полностью бесплатное решение!

- **Backend**: Render.com (бесплатный tier)
- **Frontend**: Vercel (бесплатный tier)
- **AI**: g4f (бесплатный GPT-4)

---

## 📦 Шаг 1: Деплой Backend на Render.com

### 1.1 Регистрация

1. Откройте https://render.com/
2. Нажмите **Get Started**
3. Войдите через GitHub

### 1.2 Создание Web Service

#### Вариант А: Через Dashboard (рекомендуется)

1. Dashboard → **New +** → **Web Service**
2. **Connect repository** → выберите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `mystictarot-backend`
   - **Region**: `Frankfurt` (ближе к СНГ)
   - **Branch**: `main` или `master`
   - **Root Directory**: `backend`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
   - **Plan**: `Free`

4. **Environment Variables** (нажмите Advanced):
   - `FRONTEND_URL`: (оставьте пустым, добавите после Vercel)
   - `PROXY_URL`: (оставьте пустым, попробуем без прокси)
   - `PORT`: (Render установит автоматически)

5. Нажмите **Create Web Service**

#### Вариант Б: Через render.yaml

Если в вашем репозитории есть `backend/render.yaml`, Render автоматически его использует.

### 1.3 Дождитесь деплоя

Render покажет логи деплоя:
```
==> Building...
==> Installing dependencies from requirements.txt
==> Starting service
✅ Прокси не настроен, используется прямое подключение
INFO:     Uvicorn running on http://0.0.0.0:10000
```

**Деплой занимает ~3-5 минут**

### 1.4 Получите URL

После успешного деплоя Render выдаст URL:
```
https://mystictarot-backend.onrender.com
```

**СОХРАНИТЕ ЭТОТ URL** - он нужен для Vercel!

### 1.5 Проверьте backend

Откройте в браузере:
```
https://mystictarot-backend.onrender.com/api/spreads
```

Должен вернуть JSON с 5 раскладами Таро.

⚠️ **Важно**: При первом запросе backend может просыпаться ~30 секунд!

---

## 🎨 Шаг 2: Деплой Frontend на Vercel

### 2.1 Регистрация

1. Откройте https://vercel.com/
2. Нажмите **Sign Up**
3. Войдите через GitHub

### 2.2 Создание проекта

1. Dashboard → **Add New...** → **Project**
2. **Import Git Repository** → выберите ваш репозиторий
3. Настройки:
   - **Project Name**: `mystictarot` (или любое имя)
   - **Framework Preset**: `Create React App`
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`

4. **Environment Variables**:
   - Key: `REACT_APP_API_URL`
   - Value: `https://mystictarot-backend.onrender.com` (ваш Render URL)

5. Нажмите **Deploy**

### 2.3 Дождитесь деплоя

Vercel покажет процесс:
```
Building...
Deploying...
✅ Deployment ready!
```

**Деплой занимает ~2-3 минуты**

### 2.4 Получите URL

Vercel выдаст URL:
```
https://mystictarot.vercel.app
```

или
```
https://mystictarot-username.vercel.app
```

**СОХРАНИТЕ ЭТОТ URL** - нужен для Render и Telegram!

---

## 🔄 Шаг 3: Обновите Backend

### 3.1 Добавьте FRONTEND_URL в Render

1. Откройте Render Dashboard → ваш сервис
2. **Environment** → **Add Environment Variable**
3. Добавьте:
   - Key: `FRONTEND_URL`
   - Value: `https://mystictarot.vercel.app` (ваш Vercel URL)
4. Нажмите **Save Changes**

Render автоматически передеплоит сервис (~2 минуты).

---

## 🤖 Шаг 4: Создание Telegram бота

### 4.1 Создайте бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте команду:
   ```
   /newbot
   ```
4. Введите имя бота:
   ```
   Mystic Tarot
   ```
5. Введите username (должен заканчиваться на "bot"):
   ```
   mystictarot_mini_bot
   ```

BotFather вернёт:
```
Done! Congratulations on your new bot.
Token: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**СОХРАНИТЕ TOKEN** (для будущего использования)

### 4.2 Настройте Menu Button

Отправьте BotFather:
```
/setmenubutton
```

Выберите вашего бота, затем:
```
/mybots
[Выберите бота]
Bot Settings
Menu Button
Configure menu button
```

Введите:
- **Button text**: `🔮 Открыть Таро`
- **URL**: `https://mystictarot.vercel.app` (ваш Vercel URL)

### 4.3 Настройте команды (опционально)

```
/setcommands
```

Выберите бота и введите:
```
start - Открыть Tarot Mini App
help - Помощь
```

---

## 🎯 Шаг 5: Тестирование в Telegram

### 5.1 Откройте бота

1. В Telegram найдите вашего бота: `@mystictarot_mini_bot`
2. Нажмите **Start** или `/start`
3. Внизу справа появится кнопка **Menu** (≡)
4. Нажмите кнопку Menu → **🔮 Открыть Таро**

### 5.2 Mini App откроется!

Вы должны увидеть:
- Красивый фон с анимацией
- Заголовок "Мистическое Таро"
- Поле для вопроса
- Выбор расклада

### 5.3 Создайте гадание

1. Введите вопрос: "Что меня ждёт в этом месяце?"
2. Выберите расклад: "Три карты"
3. Нажмите **Начать гадание**

⚠️ **Первый запрос может занять ~30-40 секунд** (backend просыпается на Render)

4. Карты должны перевернуться с анимацией
5. Появится толкование от GPT-4 (через g4f)

---

## ✅ Чеклист успешного деплоя

- [ ] Backend задеплоен на Render.com
- [ ] Render URL получен: `https://mystictarot-backend.onrender.com`
- [ ] `/api/spreads` возвращает JSON
- [ ] Frontend задеплоен на Vercel
- [ ] Vercel URL получен: `https://mystictarot.vercel.app`
- [ ] `FRONTEND_URL` добавлен в Render Environment
- [ ] Telegram бот создан через @BotFather
- [ ] Menu button настроен с Vercel URL
- [ ] Mini App открывается в Telegram
- [ ] Гадание создаётся и работает
- [ ] Толкование появляется (g4f работает)

---

## ⚠️ Ограничения бесплатного tier

### Render.com Free:
- ✅ 750 часов/месяц (достаточно!)
- ⚠️ **Засыпает после 15 минут бездействия**
- ⚠️ Cold start ~30 секунд при пробуждении
- ✅ 512 MB RAM
- ✅ 100 GB bandwidth/месяц

### Vercel Free:
- ✅ Полностью бесплатно
- ✅ Не засыпает
- ✅ 100 GB bandwidth/месяц
- ✅ Автоматические деплои при push в Git

### g4f:
- ✅ Полностью бесплатно
- ⚠️ Может не работать в СНГ без прокси
- ⚠️ Иногда провайдеры недоступны
- ✅ Автоматически пробует разные провайдеры

---

## 🔧 Решение проблем

### Проблема: "Backend не отвечает"

**Причина**: Backend заснул (бесплатный tier Render)

**Решение**: Подождите 30 секунд, backend проснётся автоматически.

### Проблема: "CORS error"

**Причина**: `FRONTEND_URL` не настроен в Render

**Решение**:
1. Render Dashboard → Environment
2. Добавьте `FRONTEND_URL` с вашим Vercel URL
3. Дождитесь redeploy

### Проблема: "g4f не работает / timeout"

**Причина**: Провайдер g4f заблокирован в СНГ

**Решение 1**: Попробуйте снова (g4f переключится на другой провайдер)

**Решение 2**: Настройте прокси:
1. Получите бесплатный прокси: https://www.proxy-list.download/
2. Render → Environment → добавьте `PROXY_URL`
3. Формат: `http://host:port` или `socks5://host:port`

**Решение 3**: Переключитесь на Groq API (100% работает в СНГ, бесплатно)

### Проблема: "Telegram Mini App показывает ошибку"

**Причина**: Неверный URL в menu button

**Решение**:
1. Проверьте что URL в @BotFather точно совпадает с Vercel URL
2. URL должен быть HTTPS (не HTTP)
3. Без trailing slash: `https://mystictarot.vercel.app` ✅ (не `/` в конце)

### Проблема: "Картинки карт не загружаются"

**Причина**: Папка `cards/` не загружена в репозиторий

**Решение**:
1. Убедитесь что `backend/cards/` есть в Git
2. Push в репозиторий
3. Render автоматически передеплоит

---

## 🚀 Апгрейд в будущем

Если приложение станет популярным:

### Render.com:
- **Starter**: $7/месяц - не засыпает, 512 MB RAM
- **Standard**: $25/месяц - 2 GB RAM, больше CPU

### Альтернатива для backend:
- **Railway.app**: $5/месяц
- **Fly.io**: от $0 (pay-as-you-go)

### Прокси:
- **Webshare.io**: $2.99/месяц (10 прокси)
- **Proxy6.net**: от 100₽/месяц

### AI провайдер:
- **Groq API**: бесплатно, быстрее g4f
- **DeepSeek**: от $0.14/1M tokens

---

## 📞 Полезные ссылки

- **Render Dashboard**: https://dashboard.render.com/
- **Vercel Dashboard**: https://vercel.com/dashboard
- **Telegram @BotFather**: https://t.me/BotFather
- **Бесплатные прокси**: https://www.proxy-list.download/
- **g4f GitHub**: https://github.com/xtekky/gpt4free

---

## 💡 Советы

1. **Добавьте описание бота** через @BotFather `/setdescription`
2. **Добавьте аватар** через @BotFather `/setuserpic`
3. **Проверяйте логи** Render при проблемах: Dashboard → Logs
4. **Используйте Git** для автоматических деплоев (push → auto-deploy)
5. **Мониторьте использование** Render: Dashboard → Metrics

---

**Готово! Ваше приложение теперь в Telegram! 🎉**

Все работает на бесплатных сервисах!
